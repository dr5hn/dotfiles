# ~/.zprofile
# Login shell configuration — runs once per login session
# NOTE: PATH (incl. /opt/homebrew) is set in ~/.zsh/path.zsh; fnm is lazy-loaded
#       in ~/.zsh/lazy/fnm.zsh. Both are sourced from ~/.zshrc.
